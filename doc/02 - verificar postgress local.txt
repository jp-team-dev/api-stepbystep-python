# Verifique se já existe um PostgreSQL rodando localmente:

    $ sudo lsof -i :5432

    # Return

        (.venv) hell@hell:~/projects/python/api-stepbystep-python$ sudo lsof -i :5432
        [sudo] senha para hell: 
        COMMAND   PID     USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
        apache2   750 www-data   12u  IPv6  43437      0t0  TCP localhost:40742->localhost:postgresql (ESTABLISHED)
        apache2   750 www-data   22u  IPv6  46432      0t0  TCP localhost:40764->localhost:postgresql (ESTABLISHED)
        postgres  757 postgres    6u  IPv6    811      0t0  TCP localhost:postgresql (LISTEN)
        postgres  757 postgres    7u  IPv4    812      0t0  TCP localhost:postgresql (LISTEN)
        postgres 6259 postgres   10u  IPv6  47286      0t0  TCP localhost:postgresql->localhost:40742 (ESTABLISHED)
        postgres 6261 postgres   10u  IPv6  43447      0t0  TCP localhost:postgresql->localhost:40764 (ESTABLISHED)

    # Se aparecer algum processo (por exemplo, postgres), é ele que está ocupando a porta.

        # Opções para resolver:
            
            # Pare o serviço local do PostgreSQL (se não estiver usando para outra coisa):

                $ sudo systemctl stop postgresql

                ou

                $ sudo service postgresql stop

            # Ou, se preferir, mude a porta do PostgreSQL local para outra (por exemplo, 5433) editando o arquivo de configuração do PostgreSQL (postgresql.conf) e reiniciando o serviço.

                    ports:
                        - '5433:5432'

            # E ajuste a variável DATABASE_URL do backend para usar a nova porta:

                - DATABASE_URL=postgresql://postgres:198870@db:5433/stepbystep
            
            # Rodar isso diretamente no terminal para testar a conexão com a nova porta:

                $ psql "postgresql://postgres:198870@localhost:5432/stepbystep"

                # Pra sair

                    \q
